[project]
name = "core"
version = "0.1.0"
description = "Shared domain logic (security/auth helpers) for tiny-lms"
readme = "README.md"
authors = [
    { name = "Ivan Chukhran", email = "ivan.chuhran@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "argon2-cffi>=25.1.0",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
