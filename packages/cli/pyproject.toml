[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ivan Chukhran", email = "ivan.chuhran@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "core",
    "database",
    "python-dotenv>=1.0",
]

[project.scripts]
create-admin = "cli.create_admin:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
core = { workspace = true }
database = { workspace = true }
